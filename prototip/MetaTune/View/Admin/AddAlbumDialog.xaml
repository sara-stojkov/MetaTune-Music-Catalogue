<Window x:Class="MetaTune.View.Admin.AddAlbumDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        Title="Dodavanje albuma"
        Height="850" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#FF4A4A4A"
        ResizeMode="NoResize">
    <Window.Resources>
        <!-- ComboBox Item Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FF5A5A5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF7B1FA2"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF7B1FA2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF5A5A5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                        Grid.Column="2"
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <ContentPresenter Grid.Column="0"
                                                                Content="{TemplateBinding Content}"
                                                                VerticalAlignment="Center"
                                                                Margin="10,0,0,0"/>

                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="White"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,15,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="10,0,30,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>

                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Name="DropDownBorder"
                                        Background="#FF5A5A5A"
                                        BorderThickness="0"
                                        CornerRadius="4"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <!-- DatePickerTextBox Style -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="#FF5A5A5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header Section -->
        <Grid Height="80" VerticalAlignment="Top">
            <Border Background="#FF2E2E2E">
                <TextBlock Text="Dodaj novi album" 
                          FontSize="28" 
                          FontWeight="Bold"
                          Foreground="White"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"/>
            </Border>
        </Grid>

        <!-- Main Form Content -->
        <ScrollViewer Margin="0,80,0,80" VerticalScrollBarVisibility="Auto">
            <Border Background="#FF3E3E3E" Padding="40,30">
                <StackPanel>

                    <!-- Album Name -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Naziv albuma:*" 
                                  FontSize="16" 
                                  Foreground="White"
                                  Margin="0,0,0,8"/>
                        <TextBox x:Name="AlbumNameTextBox"
                                Text="{Binding AlbumName, UpdateSourceTrigger=PropertyChanged}"
                                Height="45"
                                FontSize="16"
                                Padding="15,0"
                                Background="#FF5A5A5A"
                                Foreground="White"
                                BorderThickness="0"
                                VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <!-- Authors -->
                    <TextBlock Text="Odaberi sve autore pesme:"
                    FontSize="16" Foreground="White" Margin="0,0,0,10"/>

                    <Border Background="#FF5A5A5A" CornerRadius="4" Margin="0,0,0,25">
                        <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                            <ListBox x:Name="GenreListBox"
                        ItemsSource="{Binding Authors}"
                        SelectionMode="Multiple"
                        DisplayMemberPath="AuthorName"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        SelectionChanged="AuthorListBox_SelectionChanged">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="10,8"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <CheckBox Grid.Column="0"
                    IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                        </ScrollViewer>
                    </Border>

                    <!-- Genre Selection -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Žanr:*" 
                                  FontSize="16" 
                                  Foreground="White"
                                  Margin="0,0,0,8"/>
                        <ComboBox x:Name="GenreComboBox"
                                Height="45"
                                FontSize="16"
                                ItemsSource="{Binding Genres}"
                                SelectedItem="{Binding SelectedGenre, Mode=TwoWay}"
                                DisplayMemberPath="Name"/>
                    </StackPanel>

                    <!-- Release Date -->
                    <!-- Release Date -->
                    <StackPanel Grid.Column="1" Margin="0,20,0,0">
                        <TextBlock Text="Datum objavljivanja:*" 
                                      FontSize="16" 
                                      Foreground="White"
                                      Margin="0,0,0,8"/>
                        <DatePicker x:Name="ReleaseDatePicker"
                                       SelectedDate="{Binding PublishDate, Mode=TwoWay}"
                                       Height="45"
                                       FontSize="16"
                                       Background="#FF5A5A5A"
                                       Foreground="White"
                                       BorderThickness="0"/>
                    </StackPanel>

                    <!-- Album Description -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Opis albuma:" 
                                  FontSize="16" 
                                  Foreground="White"
                                  Margin="0,0,0,8"/>
                        <TextBox x:Name="DescriptionTextBox"
                                Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                Height="100"
                                FontSize="16"
                                Padding="15,10"
                                Background="#FF5A5A5A"
                                Foreground="White"
                                BorderThickness="0"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>

                    <!-- Editor Selection -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Izaberi editora:" 
                            FontSize="16" 
                            Foreground="White"
                            Margin="0,0,0,8"/>
                        <ComboBox x:Name="EditorComboBox"
                            Height="45"
                            FontSize="16"
                            ItemsSource="{Binding Editors}"
                            SelectedItem="{Binding SelectedEditor, Mode=TwoWay}"
                            DisplayMemberPath="Name"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Bottom Action Buttons -->
        <Grid Height="80" VerticalAlignment="Bottom">
            <Border Background="#FF2E2E2E">
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">

                    <!-- Save Button -->
                    <Button x:Name="SaveButton"
                           Content="Sačuvaj"
                           Width="150"
                           Height="50"
                           Background="#FF7B1FA2"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"
                           BorderThickness="0"
                           Cursor="Hand"
                           Command="{Binding SaveCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF6A1B9A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</Window>