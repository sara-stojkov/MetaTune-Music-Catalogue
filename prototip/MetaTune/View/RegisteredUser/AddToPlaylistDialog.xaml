<Window x:Class="MetaTune.View.User.AddToPlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Registrovani korisnik - dodavanje u plejlistu"
        Height="580" Width="460"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Border Background="#FF3E3E3E" 
            CornerRadius="8"
            BorderBrush="#FF2A2A2A"
            BorderThickness="1">
        <Grid Margin="40,30,40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Dodavanje dela u plejlistu"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>

            <!-- Form Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">

                <!-- Selected Playlist Label -->
                <TextBlock Text="Odabrana plejlista:"
                          FontSize="18"
                          FontWeight="SemiBold"
                          Foreground="White"
                          Margin="0,0,0,15"
                          HorizontalAlignment="Center"/>

                <!-- Playlist Name Display -->
                <TextBlock x:Name="PlaylistNameTextBlock"
                          Text="naziv"
                          FontSize="22"
                          FontWeight="Bold"
                          Foreground="#FF00ACC1"
                          Margin="0,0,0,40"
                          HorizontalAlignment="Center"/>

                <!-- Song Selection Label -->
                <TextBlock Text="Pesma koju dodajete:"
                          FontSize="18"
                          FontWeight="SemiBold"
                          Foreground="White"
                          Margin="0,0,0,15"/>

                <!-- Song Selection ComboBox -->
                <ComboBox x:Name="SongComboBox"
                         Height="50"
                         FontSize="16"
                         Padding="10,0"
                         Background="#FF5A5A5A"
                         Foreground="White"
                         BorderThickness="0"
                         Margin="0,0,0,50">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    BorderThickness="0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <ContentPresenter Grid.Column="0"
                                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                    VerticalAlignment="Center"
                                                                    Margin="10,0,0,0"/>

                                                    <Path Grid.Column="1"
                                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                                          Fill="White"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,15,0"/>
                                                </Grid>
                                            </Border>

                                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   Placement="Bottom"
                                                   AllowsTransparency="True">
                                                <Border Background="#FF5A5A5A"
                                                        BorderThickness="0"
                                                        CornerRadius="4"
                                                        Margin="0,2,0,0"
                                                        MinWidth="{TemplateBinding ActualWidth}">
                                                    <ScrollViewer MaxHeight="250">
                                                        <ItemsPresenter/>
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FF4A4A4A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#FF6A6A6A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Resources>
                    <!-- Songs will be populated from code-behind or ViewModel -->
                </ComboBox>

                <!-- Confirm Button -->
                <Button Content="Potvrdi"
                        Height="50"
                        FontSize="20"
                        FontWeight="Medium"
                        Background="#FF7B1FA2"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF6A1B9A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>